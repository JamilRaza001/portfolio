// PUBLIC_SITE_THEME in .env overrides this value. Restart dev / rebuild after editing.
export default {
  theme: 'black-studio',
  // Optional custom origin; Vercel's production domain is detected at build time.
  url: '',
  // Owner-supplied June 2026 CV, approved for the portfolio download.
  resume: '/resumes/muhammad-jamil-raza-resume.pdf',
};
